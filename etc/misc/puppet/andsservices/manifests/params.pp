class andsservices::params {

$repo_dir = '/tmp/ANDS-repo'

$document_root = '/var/www/html'

$mysql_root_password = 'change_this'
$mysql_old_root_password = ''  # Set this if you have an existing MySQL installation with a root password
$mysql_webuser_password = 'webuser'

$postgres_postgres_password = 'postgres' # Password for the 'postgres' user
$postgres_dba_password = 'dba'
$postgres_webuser_password = 'webuser'

}
